#%RAML 1.0 SecurityScheme
type: Basic Authentication
describedBy:
  headers:
    Authorization:
      description: used to send "username:password"
      type: string
  responses:
    401:
      description:  Either the provided username and password
            combination is invalid, or the user is not allowed to access
            the content provided by the requested URL